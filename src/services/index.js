const userService = require('./user.services');
const categoryService = require('./categories.service');
const blogPost = require('./blogPost.service');

module.exports = {
    userService,
    categoryService,
    blogPost,
};